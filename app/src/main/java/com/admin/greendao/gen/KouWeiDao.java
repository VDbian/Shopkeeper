package com.admin.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.admin.shopkeeper.entity.KouWei;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "KOU_WEI".
*/
public class KouWeiDao extends AbstractDao<KouWei, String> {

    public static final String TABLENAME = "KOU_WEI";

    /**
     * Properties of entity KouWei.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property GuId = new Property(0, String.class, "guId", true, "GU_ID");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property No = new Property(2, String.class, "no", false, "NO");
        public final static Property PatientId = new Property(3, String.class, "patientId", false, "PATIENT_ID");
        public final static Property RestaurantId = new Property(4, String.class, "restaurantId", false, "RESTAURANT_ID");
        public final static Property Own = new Property(5, boolean.class, "own", false, "OWN");
    }


    public KouWeiDao(DaoConfig config) {
        super(config);
    }
    
    public KouWeiDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"KOU_WEI\" (" + //
                "\"GU_ID\" TEXT PRIMARY KEY NOT NULL ," + // 0: guId
                "\"NAME\" TEXT," + // 1: name
                "\"NO\" TEXT," + // 2: no
                "\"PATIENT_ID\" TEXT," + // 3: patientId
                "\"RESTAURANT_ID\" TEXT," + // 4: restaurantId
                "\"OWN\" INTEGER NOT NULL );"); // 5: own
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"KOU_WEI\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, KouWei entity) {
        stmt.clearBindings();
 
        String guId = entity.getGuId();
        if (guId != null) {
            stmt.bindString(1, guId);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String no = entity.getNo();
        if (no != null) {
            stmt.bindString(3, no);
        }
 
        String patientId = entity.getPatientId();
        if (patientId != null) {
            stmt.bindString(4, patientId);
        }
 
        String restaurantId = entity.getRestaurantId();
        if (restaurantId != null) {
            stmt.bindString(5, restaurantId);
        }
        stmt.bindLong(6, entity.getOwn() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, KouWei entity) {
        stmt.clearBindings();
 
        String guId = entity.getGuId();
        if (guId != null) {
            stmt.bindString(1, guId);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String no = entity.getNo();
        if (no != null) {
            stmt.bindString(3, no);
        }
 
        String patientId = entity.getPatientId();
        if (patientId != null) {
            stmt.bindString(4, patientId);
        }
 
        String restaurantId = entity.getRestaurantId();
        if (restaurantId != null) {
            stmt.bindString(5, restaurantId);
        }
        stmt.bindLong(6, entity.getOwn() ? 1L: 0L);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public KouWei readEntity(Cursor cursor, int offset) {
        KouWei entity = new KouWei( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // guId
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // no
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // patientId
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // restaurantId
            cursor.getShort(offset + 5) != 0 // own
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, KouWei entity, int offset) {
        entity.setGuId(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setNo(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setPatientId(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setRestaurantId(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setOwn(cursor.getShort(offset + 5) != 0);
     }
    
    @Override
    protected final String updateKeyAfterInsert(KouWei entity, long rowId) {
        return entity.getGuId();
    }
    
    @Override
    public String getKey(KouWei entity) {
        if(entity != null) {
            return entity.getGuId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(KouWei entity) {
        return entity.getGuId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
